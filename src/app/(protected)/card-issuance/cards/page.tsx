"use client";

// Import the component previously used for the 'Cards' tab/list
import CardList from "@/components/card-issuance/card-list";

export default function CardIssuanceCardsPage() {
  // You might need to pass props depending on CardList's requirements
  return <CardList />;
}
